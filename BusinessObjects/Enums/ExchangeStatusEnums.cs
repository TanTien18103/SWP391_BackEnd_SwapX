namespace BusinessObjects.Enums
{
    public enum ExchangeStatusEnums
    {
        Pending = 0,      // Chờ xử lý
        InProgress = 1,   // Đang đổi pin
        Completed = 2,    // Hoàn tất
        Cancelled = 3     // Đã hủy
    }
}